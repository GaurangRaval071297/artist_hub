import 'package:flutter/material.dart';

class ArtistBookingScreen extends StatelessWidget {
  const ArtistBookingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Bookings Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
